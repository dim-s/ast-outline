apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  config.yaml: |
    nested: value
    multiline: yes
    deep:
      thing: 1
  description: >
    A folded scalar that
    becomes a single line
    when parsed by YAML.
  short: brief value

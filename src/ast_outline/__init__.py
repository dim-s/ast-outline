"""ast-outline — AST-based structural outline for source files.

Previously released as `code-outline`; renamed to `ast-outline` in v0.3.0
to join the `ast-*` family convention popularised by ast-grep. The CLI
binary `code-outline` remains a working alias through 0.4.x.
"""
__version__ = "0.4.0"
